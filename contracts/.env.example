# Foundry deploy/seed — secrets only.
# Addresses and public RPC: deployments/<chainId>.json
# Anvil scripts fall back to Anvil #0 if DEPLOYER_PRIVATE_KEY is unset.

DEPLOYER_PRIVATE_KEY=
ETHERSCAN_API_KEY=

# Optional: mainnet fork RPC for Provenance.t.sol
# RPC_URL_TARGET=
